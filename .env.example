# Supabase — shared project with eeshans.com
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=

# PostHog — proxied through Cloudflare Worker
# Values are passed at deploy time from GitHub secrets/vars.
# Fork builds work without analytics when these are absent.
PUBLIC_POSTHOG_KEY=
PUBLIC_POSTHOG_HOST=